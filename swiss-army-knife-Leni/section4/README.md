In order to efficiently check 1000 sites, it would be best to use an asynchornous 
approach. If you were to use a synchronous loop only one website would be checked
at a time. An asynchronous program checks multiple siites at a time which would
reduce execution time.

